<h1>Our team</h1>

  <div class="container text-center list-inline">
    <div class="row">
    <% data.members.each do |member, infos| %>
      <div class="col-xs-4">
        <div class="card">
          <h2><%= member %></h2>
          <%= image_tag "#{member.downcase}.jpg", class: "img-circle avatar-large" %>


        </div>
      </div>
    <% end %>
    </div>
  </div>
